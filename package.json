{
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "build:vercel": "turbo run build:vercel",
    "start": "turbo run start",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "private": true,
  "devDependencies": {
    "turbo": "^1.6.3"
  }
}
