{
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "start": "turbo run start"
  },
  "private": true,
  "devDependencies": {
    "turbo": "^1.6.3"
  }
}
